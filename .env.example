# Usado apenas em desenvolvimento local (npm run dev / npm run build local).
# Em produção (imagem Docker), a URL da API é injetada em runtime pela variável
# de ambiente API_BASE_URL do container — ver docker/docker-entrypoint.sh e a
# seção "Deploy (Docker)" do README.
VITE_API_BASE_URL=http://localhost:8080
